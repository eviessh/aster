attributes: interpreted.
imports;    build, cmd.

buildArguments is struc build-ARGUMENTS = {}.

foreach; argument, cmd-arguments
{
    match argument
    {
        build-standardArguments(buildArguments).
    }.
}
