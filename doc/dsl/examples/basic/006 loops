import; basic, io.

noYield: loop[i: U8 = 0] =
{
    io-printU8 i.
    io-print "\n".

    i++.
    if[i ?>= 100] = yield.
}

a: loop[i: U8 = 0] -> U8 =
{
    io-printU8 i.
    io-print "\n".

    i++.
    if[i ?>= 100] = yield i.
}
io-printU8 a.

